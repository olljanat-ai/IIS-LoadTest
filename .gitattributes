# This is a Windows/IIS project: keep the deployed files in CRLF so they read
# correctly in Notepad, the IIS Manager editors and the ISE.
* text=auto

*.cs      text eol=crlf
*.aspx    text eol=crlf
*.ashx    text eol=crlf
*.config  text eol=crlf
*.ps1     text eol=crlf
*.md      text
